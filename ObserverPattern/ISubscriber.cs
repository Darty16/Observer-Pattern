// Observer interface
// 'ISubscriber' represents the subscriber interface 
// Defines the 'Update' method for receiving updates
public interface ISubscriber
{
    // Method for receiving updates
    void Update(string videoTitle);
}
